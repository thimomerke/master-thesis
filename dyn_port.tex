\section{Optimal Dynamic Portfolio}

While the static portfolios are valuable to understand the tradeoffs between the various NET's in regards to cost and speed of deployment, they are of limited use in providing policy recommendations for the goal set in the Paris agreement of limiting global warming to below 2 °C. In order to achieve this goal, the UN Environment Programme has estimated CDR targets of 3.8 Gt annually by 2035, increasing to 9.2 Gt by 2050 and 20 Gt by 2100 \parencite{unep_emissions_2023}. In order to appropriately reflect these targets, a dynamic optimization approach, which considers the need to continually remove an ever-increasing amount of CO$_2$, is required.\vspace{2mm}

The dynamic portfolio was developed from the same fundamental constraints applied to the static optimization. The primary difference is that instead of a fixed amount of CO$_2$ to be removed over a set timeframe, a segmented, linearly increasing function was used, starting from zero in the year 2026 and ramping up in four segments to reach the above-mentioned CDR targets. Additionally, due to the dynamic nature of the model, the costs of each NET over the entire time frame were discounted to the starting year.\vspace{2mm}

\subsection{Model Formulation}
\subsubsection{Objective function}
In the dynamic optimization model, the time period $ T =  \{0,1,...,T\} $ is defined, where $t=0$ corresponds to the start year $y_0$, and $t=T$ to the final year $y_T$. Smiliar to the static model, the dynamic model determines the least-cost portfolio of afforestation, BECCS, enhanced weathering, and direct air capture. However, the removal target $R$ is not defined as one cumulative amount, but rather an annual removal trajectory, generated by linear interpolation between the given targets. Additionally, the dynamic model uses discounted cost values to properly account for the time value of money. The value of carbon removed is not discounted, with atmospheric carbon considered to have the same effect irrespective of time.\vspace{2mm}

The optimization problem can thus be written as
\begin{equation}
\begin{aligned}
\min \sum_{t\in T}\gamma_t \Big[ 
& \underbrace{\sum_{c\in \mathcal{C}^1} k^1_c x_{t,c}^1}_{\text{A/R}} \\[4pt]
& + \underbrace{\sum_{(c,b) \in \mathcal{P}} \Big(
    I^2 x_{t,c}^2 
    + k^{\text{2,f}} X_{t,c}^2 
    + k_{c,b}^{bm} u_{t,c,b}^{bm} }_{\text{BECCS}} \\
& \quad \underbrace{
    + \left( k_{c,b}^{2, v} + k_{c,b}^{2, ts} \right) q_{t,c,b}^{2,\text{gross}} 
    - u_{t,c,b}^{bm} \eta_{c,b}^{pow} p_c^\text{pow}
\Big)}_{\text{BECCS (cont.)}} \\[4pt]
& + \underbrace{F^3(q_t^3)}_{\text{ERW}} \\[4pt]
& + \underbrace{
    q_t^\text{4,gross} (k^{4,v}  + k^{4,ts})
    + X^4 k^{4,f} 
    + x_t^4 I_t^4
}_{\text{DAC}}
\Big]
\end{aligned}
\end{equation}

%\begin{equation}
%  \text{min} \sum \gamma_t \left[ K^{\text{A/R}}_t + K^{\text{BECCS}}_t + K^{\text{ERW}}_t + K^{\text{DAC}}_t \right]
%\end{equation}
where $\gamma_t = \frac{1}{(1+r)^t}$ is the discount factor.\vspace{2mm}

The technology models remain the same, with the exception of a re-worked BECCS model that is more similar in design to the DAC model, in order to separate the capacity investment and operating decisions.

\subsubsection{Bioenergy with Carbon Capture and Storage}
Compared to the static model, the BECCS implementation in the dynamic model uses capacity expansion and operation as separate decision variables, similar to the DAC model.\vspace{2mm}

Let $\mathcal{C}^2$ be the set of countries with BECCS potential, and $\mathcal{B}$ the set of biomass types available. Then, $\mathcal{P}\subseteq\mathcal{C}^2\times\mathcal{B}$ is the set of country-biomass pairs available. For each $(c,b) \in \mathcal{P}$, let $a_{c,b}$ be the constant amount of biomass available per country, $\eta_{c,b}^{cap}$ the captured CO$_2$ per unit of biomass, $\eta_{c,b}^{pow}$ the electricity exported per unit of biomass, $k_{c,b}^{bm}$ the cost per unit of biomass, and $k_{c,b}^{var}$ and $k_{c,b}^{ts}$ the variable cost and transport and storage cost, respectively, per captured unit of CO$_2$.\vspace{2mm}

The installed BECCS capacity evolves analogously to the DAC model, as
\begin{equation}
X_{t,c}^2 = \sum^{t-1}_{\tau=\text{max}(0,t-L^2)} x_{\tau,c}^2    \qquad \forall T \backslash\{ 0 \}, c \in \mathcal{C}^2
\end{equation}
where $L^2$ is the assumed plant lifetime. The capacity expansion is again subject to an initial build limit and an annual growth constraint proportionate to the previous years' capacity, denoted in MW gross electricity generation:
\begin{equation}
x_{t,c}^2 \leq
      \phi^2X_{t-1,c}^2 \qquad \forall t \in T, c \in \mathcal{C}^2
\end{equation}
where $\phi^2$ is the annual growth limit relative to the previous periods capacity. The biomass use is limited by the resource availability:
\begin{equation}
u_{t,c,b}^\text{bm} \leq a_{c,b}^\text{bm} \qquad \forall t \in T \backslash\{ 0 \}, (c,b) \in \mathcal{P}.
\end{equation}
Gross carbon captured is then proportional to the biomass throughput:
\begin{equation}
q_{t,c,b}^\text{3,gross} = \eta_{c,b}^{cap}u_{t,c,b}^{bm} \qquad \forall t \in T, (c,b) \in \mathcal{P}.
\end{equation}
The installed BECCS power capacity limits the annual biomass throughput through an energy-output constraint:
\begin{equation}
\sum_{(c,b)\in \mathcal{P}} u_{t,c,b}^{bm}\eta_{c,b}^{pow} \leq X_{t,c}^2h^\text{FLH}\lambda^{2}(1-\ell^{aux})  \qquad \forall t \in T
\end{equation}
where $\mathcal{B}_c$ denotes the set of biomass types availabile in country $c$, $h^\text{FLH}$ the theoretical full-load hours, $\lambda^2$ the average capacity factor, and $\ell^{aux}$ the auxiliary energy loss.\vspace{2mm}

The net BECCS carbon removal per year are finally computed as the amount of CO$_2$ net of the leakage:
\begin{equation}
q_{t}^2 = (1-\alpha^2) \sum_{(c,b)\in \mathcal{P}}q_{t,c,b}^\text{3,gross} \qquad \forall t \in T.
\end{equation}
where $\alpha^2$ is the BECCS leakage factor.\vspace{2mm}

The annual costs of BECCS are then calculated as the sum of capital expenditures, fixed operating costs, and variable costs, less revenue from electricity export. Specifically,
\begin{equation}
K_t^\text{2,cap} = \sum_{c \in \mathcal{C}^2} I^2x_{t,c}^2,
\end{equation}
\begin{equation}
K_t^\text{2,f} = \sum_{c \in \mathcal{C}^2} k^\text{2,f}X_{t,c}^2,
\end{equation}
\begin{equation}
K_t^\text{2,v} = \sum_{(c,b) \in \mathcal{P}} \left[ k_{c,b}^{bm}u_{t,c,b}^{bm} + \left( k_{c,b}^v + k_{c,b}^{ts} \right) q_{t,c,b}^\text{gross} \right],
\end{equation}
with electricity revenue
%\begin{equation}
%Rev_t^\text{2,pow} = \sum_{c \in \mathcal{C}^2} X_{t,c}^2h^\text{FLH}\lambda^2(1-\ell^\text{aux})p_c^\text{pow}.
%\end{equation}
\begin{equation}
Rev_t^\text{2,pow} = \sum_{(c,b) \in \mathcal{P}} u_{t,c,b}^{bm} \eta_{c,b}^{pow} p_c^\text{pow}.
\end{equation}

Note that the fixed cost is calculated based on the total installed capacity, irrespective of actual operation, while variable cost is based on the amount of biomass consumed and the additional variable costs per unit of CO$_2$ captured. The total BECCS cost in year $t$ is then

\begin{equation}
K_t^2 = K_t^{\text{2,cap}} + K_t^{\text{2,f}} + K_t^{\text{2,v}}- Rev_t^{\text{2,pow}}.
\end{equation}

\subsection{Model Implementation}

The implementation again follows the implementation of the static model as described above. The discount factor $r$ is set at 7\%, with the starting year $y_0 = 2026$ and the final year $y_T = 2101$, creating a 75 year planning horizon. The intermediate targets are set as $R_{2031} = 2.500$, $R_{2036} = 3.800$, $R_{2051} = 9.200$, $R_{2101} = 20.000$ MtCO$^2$ yr$^{-1}$.\vspace{2mm}

For the new BECCS model, the initial electric capacity ($x_{0,c}^2$) is considered to be 1000 MW for each country, and the annual increase limit ($\phi^2$) is set to 10\%. The theoretical full-load hours ($h^\text{FLH}$) per year are assumed to be 8760, with an average capacity factor ($\lambda^2$) of 85\%. The auxiliary energy loss ($\ell^{aux}$) is set at 27\%, and the leakage factor ($\alpha^2$) at 24\%, based on upstream emissions, flue gas not captured, and storage leakage.\vspace{2mm}

\subsection{Results}

\begin{figure}[ht!]
  \begin{center}
    \includegraphics[width=1\linewidth]{figures/optimal_dynamic_portfolio.png}
    \caption{Optimal dynamic portfolio (up to year 2100)}
    \label{fig:optimal_dynamic_portfolio}
  \end{center}
  \vspace*{-\baselineskip}
\end{figure}

The resulting portfolio, shown in Figure 4.3, is the direct answer to the second research question posed in Section 1.3 ("What is the cost-minimizing global portfolio of NETs to meet the goals of the Paris agreement of limit global warming to "well below" 2 °C?"). The A/R approach is immediately deployed to its maximum potential, constrained only by the annual expansion limit and the overall available land area. Additionally, BECCS is also immediately deployed to cover the remaining CDR gap that A/R cannot fulfill, and is ramped up until it reaches its maximum as defined by the available waste biomass. Direct Air Capture only starts operating once A/R and BECCS are near their maximum, but later carries the bulk of the CDR efforts from the mid 2060s onwards. Enhanced Weathering is also deployed from the get go, but only plays a very limited role. In total, this portfolio includes 879 Gt of CDR until 2100 at a total cost of 177T USD.\vspace{2mm}

\begin{table}[h!]
\adjustbox{max width=\textwidth}{%
\renewcommand{\arraystretch}{1.4}
\begin{tabular}{lrrr}
\hline
\textbf{Technology} & \textbf{Amount removed (Mt)} & \textbf{Total cost (B USD)} & \textbf{Average cost (USD)} \\ \hline
\textbf{A/R}   & 105{,}686.91 & 1{,}089.31  & 10.31 \\
\textbf{ERW}   & 46{,}154.01  & 1{,}284.19  & 27.82 \\
\textbf{BECCS} & 287{,}952.15 & 51{,}188.09 & 177.77 \\
\textbf{DAC}   & 438{,}966.92 & 123{,}694.91 & 281.79 \\ \hline
\textbf{Total} & 878{,}759.99 & 177{,}256.50 & 201.71 \\ \hline
\end{tabular}}
\caption{Breakdown of amounts removed and cost by NET}
\end{table}

Figure 4.4 shows the development of the average cost, with ERW staying nearly constant, while the costs of A/R and DAC drop significantly and the cost of BECCS actually increases over time.\vspace{2mm}

\begin{figure}[h!]
  \begin{center}
    \includegraphics[width=1\linewidth]{figures/annual_lcoc_by_tech.png}
    \caption{Average cost development over time}
    \label{fig:dac_capex_learning}
    \vspace*{-\baselineskip}
  \end{center}
\end{figure}

Finally, looking at the development of the capital expenditures per unit and capacity construction for DAC in Figure 4.5, we see a significant drop from the initial price of 1800 USD per tonne to 800 USD by 2040, and about 500 USD by 2060. We also see that investment in and construction of DAC begins immediately, even when it is not operated until the 2040s, and afterwards spikes in 20 year corresponding to the unit lifetime.\vspace{2mm}

\begin{figure}[h!]
  \begin{center}
    \includegraphics[width=1\linewidth]{figures/dac_capex_learning.png}
    \caption{DAC CapEx learning curve over time}
    \label{fig:dac_capex_learning}
    \vspace*{-\baselineskip}
  \end{center}
\end{figure}

Note that the cost values reported here are implied average costs derived from total costs divided by total CDR, and do not account for the time value of money or CO$_2$ removed. While these values are comparable to the LCOC estimates reported in the literature and the models constructed above, they are the result of the optimization model and not of the LCOC formula 3.1.\vspace{2mm}

A number of alternative scenarios, such as the the exclusion of A/R, use of forest plantations instead of assisted natural regeneration, or an increased availability of ERW, were considered and compared to the baseline scenario in Appendix 6.